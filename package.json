{
    "name": "person-gen",
    "version": "1.0.0",
    "description": "Generates fake person data using Faker.js",
    "scripts": {
        "dev": "npx --package=tailwindcss tailwindcss -i ./css/tailwind-source.css -o ./css/style.css --watch",
        "build": "npx --package=tailwindcss tailwindcss -i ./css/tailwind-source.css -o ./css/style.css --minify"
    },
    "devDependencies": {
        "tailwindcss": "^4.0.0-alpha.13"
    }
}
